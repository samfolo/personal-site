/**
 * Storage Key Configuration
 *
 * Centralised localStorage key definitions.
 * All storage keys use the sf.site.* namespace to avoid collisions.
 */

/**
 * localStorage key constants.
 */
export const STORAGE_KEYS = {
  THEME: "sf.site.theme",
} as const;
