---
/**
 * Boids Component
 *
 * Animated background canvas using p5.js boids simulation.
 * Fixed position at z-index 0, behind all content.
 * Responds to theme changes and scroll position.
 */

import {DOM_IDS} from "../../config/dom/selectors";
---

<div id={DOM_IDS.BOIDS_CANVAS} class="boids-canvas"></div>

<script src="../../scripts/boids.ts"></script>

<style>
  .boids-canvas {
    position: fixed;
    inset: 0;
    z-index: var(--z-base);
    pointer-events: none;
  }

  /* Ensure canvas inside fills container */
  .boids-canvas :global(canvas) {
    display: block;
    width: 100%;
    height: 100%;
  }
</style>
